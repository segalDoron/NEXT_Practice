import React, { Fragment } from 'react'
import styled from 'styled-components';

const RestaurantsList = ({ addressList, header, onClick, ...otherProps }) => (

    <Fragment>
        <div></div>
    </Fragment>
);

export default RestaurantsList;
